# Data licence record

Status: `NOT_RUN`.

No citation-support corpus is frozen in this checkout. A release must attach a
per-source URI, byte digest, licence/right basis, attribution, allowed-use
statement, and immutable approval record before any pair is admitted to a
training, calibration, locked-test, OOD, or temporal split.
