# Adjudication protocol

Two independent annotators label each exact claim/span pair. A discipline
competent adjudicator resolves disagreements without seeing model output,
records the competing labels and rationale, and signs the immutable manifest.
Agreement, per-label counts, per-discipline counts, licence evidence, and
leakage checks are release evidence. Missing human records produce `NOT_RUN`.
