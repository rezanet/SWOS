# Source-diversity locked packets

This directory is reserved for the separately approved tuning and locked
human-reviewed packets. The checked-in manifest intentionally contains zero
packets and reports `NOT_RUN`; synthetic fixtures may not satisfy the release
gate.
