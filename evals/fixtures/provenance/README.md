# PROV certification fixtures

The release corpus must contain permitted, checksummed valid, invalid, large,
and adversarial PROV cases. This checkout records the corpus as `NOT_RUN` until
those artefacts and an independent oracle are supplied.
