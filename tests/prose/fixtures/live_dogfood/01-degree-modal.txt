The current implementation provides a useful foundation for semantic-safe rewriting, however the way in which the different components interact with one another is still somewhat difficult to understand when reading the system for the first time. This is particularly the case because the verifier, rewriter, deterministic checks and provider contracts each perform different roles, and these roles are important for understanding how the system prevents meaning from being changed while the prose itself is being improved.
