# SWOS Prose live dogfood fixtures

These short synthetic paragraphs are intentionally public and contain no private, unpublished, or copyrighted source material. They exercise degree/modality, uncertainty, causality/attribution, technical terminology, and numeric/citation preservation.

The trusted `SWOS Prose Live` workflow runs them through the real rewrite and semantic-verifier providers with `gpt-5.6-luna` and uploads the JSON results as a short-lived Actions artifact. The fixture corpus is empirical evidence, not a formal benchmark and not a release gate by itself.
